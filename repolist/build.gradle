apply plugin: "com.android.library"

project.configureBase(this)
project.configureHilt(this)
project.configureCompose(this)

dependencies {
    api projects.backend
    api projects.core
    api projects.repodetail
}
